This project implements an AI-enabled system to predict both visa approval status and estimated processing time using historical visa application data. The dataset is preprocessed through cleaning, encoding, and feature engineering, followed by data visualization to understand key trends. Machine learning models are trained for classification to predict visa status and regression to estimate processing time. The final solution integrates these models into a web interface that allows users to input application details and receive predicted visa status along with expected processing duration.
